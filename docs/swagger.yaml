info:
  contact: {}
paths:
  /basic/ping:
    get:
      consumes:
      - application/json
      description: Responds with pong
      produces:
      - application/json
      responses:
        "200":
          description: pong
          schema:
            type: string
      summary: Ping example
      tags:
      - example
swagger: "2.0"
